[env:Pico16MB-GC9A01]
board = ${pico16mb.board}
board_build.filesystem_size = ${pico16mb.board_build.filesystem_size}
platform = ${pico16mb.platform}
build_flags = 
	${GC9A01.build_flags}
	${pico16mb.build_flags}
upload_port = .pio/build/Pico16MB-GC9A01/
