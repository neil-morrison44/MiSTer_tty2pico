[env:Pico16MB-ILI9341]
board = ${pico16mb.board}
board_build.core = ${pico16mb.board_build.core}
board_build.f_cpu = ${pico16mb.board_build.f_cpu}
board_build.filesystem_size = ${pico16mb.board_build.filesystem_size}
build_unflags = ${pico16mb.build_unflags}
platform = ${pico16mb.platform}
build_flags = 
	${ILI9341.build_flags}
	${pico16mb.build_flags}
upload_port = .pio/build/Pico16MB-ILI9341/
